<div class="row">
	<div class="col-md-12">
		<% flash.each do |type, msg| %>
			<% map = { error: "danger", notice: "info", success: "success"} %>
			<div class="alert alert-<%= map.dig(type.to_sym) %>"><%= msg %></div>
		<% end %>
		<% flash.clear %>
	</div>
</div>
